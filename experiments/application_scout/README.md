# Application scout experiments

Configuration-driven application experiments belong here. Each run must retain
its seed, data source/version, split identity, preprocessing fit scope, model
parameters, runtime, metrics, and outcome. Raw and GB variants must share the
same split and tuning budget.

