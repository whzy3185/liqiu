# Research log — 2026-08-24

## New papers

Crossref discovery completed across 32 recent and 5 foundational queries. After
DOI/title deduplication and a high-precision title filter, 160 unique papers were
retained: 154 from 2019–2026 and 6 earlier theory/method records. Semantic Scholar
abstract enrichment supports abstract-level coding for 73 records; 87 remain
explicitly metadata-only.

## New collisions

No candidate novelty verdicts were issued. First-pass occupied clusters include
granular-ball rough/neighborhood rough sets, granular-ball feature selection,
granular-ball three-way decisions, and sequential three-way decisions.

## New failures

Infrastructure/search-pipeline failures only; none count as scientific evidence:

1. Direct execution initially failed to import the repository package. The entry
   point now inserts the repository root and is tested.
2. Crossref rejected an unsupported select field. The request was narrowed to
   verified fields and API error messages are now validated.
3. Semantic Scholar returned a rate-limit object where a paper list was expected.
   Enrichment now degrades to metadata-only and discovery results are cached.
4. Naive `OOD` substring coding matched `neighborhood`. The abbreviation match was
   removed; only explicit open-set/open-world/out-of-distribution phrases count.

No scientific failure pattern has been promoted. Fourteen synthetic families now
exist, but they have only passed generator validation.

Campaign v1 subsequently completed 72/72 exploration runs over 12 parameter
regions, original/adaptive author GBC code, and seeds 1/7/21. High-dimensional
XOR is the leading replication candidate; no result is yet a research hypothesis.
The campaign also falsified naive use of loss ratio near a zero reference loss.

## New hypotheses

None promoted. H1–H8 from the project brief remain test prompts, not findings.

## Experiments completed

TASK 0 deterministic smoke test only. It is reproducible and is explicitly marked
as infrastructure-only rather than scientific evidence.

Two author-code structure smoke tests were then completed on generated moons:

- original GBC generation: `20260824T095642Z-9b06be41c5e4`;
- adaptive GBG: `20260824T095755Z-aa2b5e62ddf0`.

They verify executable structure generation, not paper-level accuracy.

Three more core paths now run: GBRS feature reduction, GBFRS feature ranking,
and S3WD-GBRS three-way prediction. GBFRS first failed because upstream wildcard
NumPy import shadowed built-in `max`; the failure remains in JSONL and the later
success records compatibility shims. The five-path runnable gate is met, while
paper-table reproduction remains open.

## Candidates killed

None.

## Candidates promoted

None.

## Unexpected findings

The retained records rise sharply in 2024–2026, and multiple 2026 records already
occupy granular-ball rough-set, three-way, incremental, and feature-selection
combinations. This increases the need for primary-source verification before any
novelty claim.

## Plan changes

No change to the requested plan. JSON was chosen for executable experiment
configuration because it is supported by Python's standard library, avoiding a
premature dependency on a YAML parser.

Automated component coding was added as a triage layer, but unknown fields remain
unknown. This modifies the practical interpretation of TASK 2: the matrix has 160
rows, while full component completeness is deferred to a representative full-text
audit rather than being hallucinated from titles.

## Next actions

1. Verify foundational and highly connected papers against primary sources.
2. Search and audit official/public baseline repositories.
3. Upgrade split/merge/stop, datasets, baselines, gains, and limitations for the
   representative full-text tranche.
4. Select at least five GBC/3WD baselines based on verified code and influence.

The first audit selected original GBC, adaptive GBG, GBRS, GBFRS,
3WC-GBNRS++, and S3WD-GBRS. GBG++ and local-density GBG remain mandatory search
targets but currently lack a verified public repository.

The counterexample lab now has 12 deterministic geometry families plus shared
noise, outlier, imbalance, density, and ambient-dimension controls. Streaming
drift remains pending.

Next, target XOR across dimensions 2–500, overlap and projection variants with
all five initial seeds. Absolute accuracy gap becomes the primary ranking metric;
loss ratio is reported only with a denominator floor.

XOR v1 then completed 140/140 runs. The high-dimensional explanation was
falsified; overlap 0.25 produced the stable common gap (69/70 negative runs).
O-001 is now cross-method replicated within one synthetic family, while PH-001
is refuted and replaced by pre-hypothesis PH-002. No method has been proposed.

Alternating v1 added 90/90 independent-generator runs. Sector wheels and
checkerboard-4 replicate both-method gaps; compact Gaussian XOR is a strong
counterexample that rules out disconnected class regions alone. PH-002 is now
cross-generator replicated but stays pre-hypothesis until a causal statistic and
real-data analogue are established.

A 230-run mechanism audit found only moderate impurity–gap association and a
stronger but confounded fragmentation signal. Original GBC's low-purity flag is
zero for all 115 targeted runs despite held-out gaps, establishing purity-stop
blindness as an observation rather than proving a new mechanism.

Author `gb_knn.py` audit then found that v1 targeted campaigns used nearest-center
instead of the official center-distance-minus-radius prediction. V1 was retained
and deprecated. A clean-room classifier matched author ball sizes and predictions,
and corrected v2 completed 230/230 runs. The overlap/sector signal survives but
with materially smaller gaps; all current claims now use v2.

Five public OpenML datasets added 25 fixed-purity runs, followed by a 105-run
purity scan. The scan discovered three regimes—under-splitting phase transition,
accuracy-neutral explosion, and harmful over-refinement—plus Banknote/Sonar
counterexamples where high purity helps. PH-003 is cross-dataset but remains
pre-hypothesis pending adaptive/GBG++/local-density replication.

Author accelerated-GB added a second 105-run purity scan. The under-splitting
transition and accuracy-neutral explosion reproduce across methods; incompatible
dataset optima persist. PH-003 is promoted to H-003, the first research
hypothesis. No repair mechanism is retained before novelty search.

Twenty H-003 mechanisms were generated and scored. Deepening the novelty search
immediately found a classification-specific local MDL granular-ball paper
(arXiv:2605.11406); M05 was marked HIGH_COLLISION and rejected. The structured
corpus now contains 162 records. Remaining top mechanisms are still UNKNOWN.

M01 then entered a 15-run Cheap Test as a low-novelty kill test. Its one-sided
confidence bound improves two datasets but worsens Ionosphere and adds hundreds
of low-value balls on Electricity/Phoneme. M01 is rejected; finite-sample
confidence must influence a cost-aware three-way action rather than uniformly
forcing more splits.

M02 global negative-control v1 exposed and retained a double-scaling bug; v2
reran 45 experiments with new IDs. Validation/cost selection helps Banknote,
Phoneme and Electricity cost, but is unstable on Ionosphere/Sonar. M02 remains
lower-priority and now requires resampling stability.

M04 added a 45-run nested Brier/cost control. It reveals a genuine
calibration–accuracy–structure tradeoff but fails as a single objective,
especially on Ionosphere/Sonar. M04 is demoted to a constraint component.
