# Problem

Does releasing granular-ball statistics reduce tabular privacy leakage at equal
utility, and does it do so better than an ordinary matched partition?

The direction is falsified if GB behaves like KMeans after matching the number
of representatives. A negative result is still potentially useful if small,
high-purity, or low-radius GBs show a stable GB-specific leakage pattern.

