# Problem taxonomy

Initial stress-test axes: geometry, label/feature noise, imbalance, distribution
shift, high dimensionality, calibration, structure instability, runtime, and
memory. This taxonomy will be revised when repeated limitations in primary
sources or experiments reveal a missing category.

